# rusty

Learning Rust via [the Book](https://doc.rust-lang.org/stable/book/).

**At:** [Ch. 4 — Ownership](https://doc.rust-lang.org/stable/book/ch04-01-what-is-ownership.html) · **Goal:** a multithreaded web server.

## Run

Install Rust ([rustup](https://rustup.rs)), then in any project:

```bash
cargo run
```

`Cargo.lock` is committed, so builds are reproducible.

## Projects

`hello_world` · `hello_cargo` · `variables` · `functions` · `branches` · `guessing_gamer` · `ownership`
